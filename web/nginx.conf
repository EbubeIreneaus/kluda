server {
    listen 80;
    server_name kluda.app www.kluda.app localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
